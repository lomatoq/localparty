const fs=require('fs');let p='games/party/public/host.js',s=fs.readFileSync(p,'utf8');s=s.replace('if(dead){ctx.translate(0,5);ctx.rotate(-1.15);','if(dead&&!shot){ctx.translate(0,5);ctx.rotate(-1.15);');s=s.replace('const target=poses[poses.length-1-i]||pose,end=', 'const target=poses.filter(q=>q.y===y&&flip*(q.x-x)>0).sort((a,b)=>Math.abs(a.x-x)-Math.abs(b.x-x))[0]||pose,end=');fs.writeFileSync(p,s);
s=fs.readFileSync('tests/game-art-browser.cjs','utf8');s=s.replace("if(['push','shrink','knives','bomb','western','western_duel','kart'].includes(id))await host.waitForTimeout(2500);",`const pf=phone.frames().find(f=>f.url().includes('/games/'+id+'/'));await gameFrame.evaluate(()=>{window.shotFrames=[];window.shotCheck=[];let seen='';function record(){const party=typeof westernPositions==='function',d=party?state?.players?.find(p=>p.shotThisRound&&!p.falseStart):state?.duel?.shots?.find(p=>!p.early);const key=d?(party?state.game.round+':'+d.id:state.duel.id):'';if(key&&key!==seen){seen=key;window.shotCheck.push(party?{game:'western',poses:westernPositions(state.players.filter(p=>p.active).length),shot:d}: {game:'western_duel',shot:d,duel:state.duel});for(const delay of [0,80,180,360])setTimeout(()=>shotFrames.push({delay,image:document.querySelector('canvas').toDataURL('image/png')}),delay);}requestAnimationFrame(record);}requestAnimationFrame(record);});await pf.evaluate(()=>{window.autoShot=setInterval(()=>{if(typeof self!=='undefined'&&typeof socket!=='undefined'&&self?.mode==='western'&&self.westernPhase==='draw'&&!self.shotThisRound)socket.emit('westernShoot',{round:self.round});else if(typeof state!=='undefined'&&state?.phase==='draw')send('shot',{duelId:state.duel.id});},8);});await gameFrame.waitForFunction(()=>shotFrames.length>=4,null,{timeout:45000});const frames=await gameFrame.evaluate(()=>({frames:shotFrames.slice(0,4),checks:shotCheck}));for(const frame of frames.frames)fs.writeFileSync('tests/actual-shot-'+id+'-'+frame.delay+'.png',Buffer.from(frame.image.split(',')[1],'base64'));fs.writeFileSync('tests/actual-shot-'+id+'.json',JSON.stringify(frames.checks,null,2));await pf.evaluate(()=>clearInterval(autoShot));`);fs.writeFileSync('tests/western-facing-browser.cjs',s);
