const fs=require('fs');const helper=`\nfunction tankArtwork(context,r,color){const art=window.PartyArt;if(!art?.sprite('tank-body',color)||!art.sprite('tank-turret',color))return false;art.draw(context,'tank-body',0,0,r*2.3,r*2.7,{color,rotation:Math.PI/2});art.draw(context,'tank-turret',0,0,r*1.6,r*2.7,{color,rotation:Math.PI/2,pivot:{x:.5,y:.76}});return true;}\n`;
let p='games/tanks/public/host.js',s=fs.readFileSync(p,'utf8');s=s.replace('function drawTank(p){',helper+'function drawTank(p){');s=s.replace("  ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(0,0,p.radius,0,Math.PI*2);ctx.fill();", "  if(!tankArtwork(ctx,p.radius,p.color)){ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(0,0,p.radius,0,Math.PI*2);ctx.fill();");s=s.replace("ctx.arc(0,0,5,0,Math.PI*2);ctx.fill();ctx.restore();", "ctx.arc(0,0,5,0,Math.PI*2);ctx.fill();}ctx.restore();");fs.writeFileSync(p,s);
p='games/tankarena/public/app.js';s=fs.readFileSync(p,'utf8');s+=helper;s=s.replace("g.rotate(p.angle);g.fillStyle='#090f16';", "g.rotate(p.angle);if(!tankArtwork(g,22,p.color)){g.fillStyle='#090f16';");s=s.replace("g.arc(0,0,12,0,Math.PI*2);g.fill();g.restore();", "g.arc(0,0,12,0,Math.PI*2);g.fill();}g.restore();");fs.writeFileSync(p,s);
