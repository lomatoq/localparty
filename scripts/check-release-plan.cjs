const fs=require('fs'),vm=require('vm'),path=require('path'),assert=require('assert/strict');const filename=path.resolve('scripts/package-release.cjs'),source=fs.readFileSync(filename,'utf8').split('(async()=>')[0];const sandbox={require,__dirname:path.dirname(filename),console};vm.createContext(sandbox);vm.runInContext(source+';globalThis.check=include;globalThis.version=VERSION;',sandbox);for(const f of ['public/assets/games/premium-v2/sources/draft.png','LOCAL_PARTY/certs/local.key','public/.env.local','data/profiles.json','public/assets/games/flat-test/rejected.png','LOCAL_PARTY/public/assets/gameplay/sources/raw.png','public/assets/gameplay/crane-city-source.png'])assert(!sandbox.check(f),f);const m=JSON.parse(fs.readFileSync('public/assets/gameplay/manifest.json'));for(const frame of Object.values(m.frames)){for(const key of ['atlas','tintMask'])if(frame[key]){const f='public/assets/gameplay/'+frame[key];assert(sandbox.check(f),f);assert(fs.existsSync(f),f);}}assert.equal(sandbox.version,'0.6.0-alpha');console.log('PASS release exclusions and all manifest-referenced assets retained');
