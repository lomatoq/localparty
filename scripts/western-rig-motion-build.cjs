const fs=require('fs');let s=fs.readFileSync('tests/western-facing-live-hit.cjs','utf8'),a=s.indexOf('await gameFrame.waitForFunction(()=>typeof westernPositions'),b=s.indexOf('await pf.evaluate(()=>{window.autoShot=',a);s=s.slice(0,a)+s.slice(b);s=s.replace('[0,80,180,360,1400,1700,2300]','[0,40,80,120,160,240,600,1800]').replace('shotFrames.length>=7','shotFrames.length>=8').replace('shotFrames.slice(0,7)','shotFrames.slice(0,8)');fs.writeFileSync('tests/western-rig-motion.cjs',s);
