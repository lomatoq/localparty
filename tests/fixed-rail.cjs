const {chromium}=require('C:/Users/nirrt/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1920,height:1080}});await p.goto('http://localhost:59435/host');await p.locator('.game').first().waitFor();await p.waitForTimeout(700);const rects=()=>p.evaluate(()=>['.company','#qrDock','.evening-console','.app-header'].map(s=>{const r=document.querySelector(s).getBoundingClientRect();return {top:r.top,bottom:r.bottom}}));const before=await rects();await p.evaluate(()=>scrollTo(0,900));await p.waitForTimeout(700);assert.deepEqual(await rects(),before);assert(before[0].bottom<before[1].top);assert(Math.abs(before[0].top-before[2].top)<1);assert.equal(await p.locator('.ambient').evaluate(e=>getComputedStyle(e,'::after').display),'none');await p.screenshot({path:'tests/fixed-rail.png'});console.log('PASS fixed room/top and QR positions; no overlap; grain removed');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
