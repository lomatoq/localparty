if(['jenga','kart','tanks','western_duel','spy'].includes(process.env.PARTY_GAME_ID)){const r=require('../lib/party-runtime');const original=r.now;const start=original();const fast=()=>start+(original()-start)*40;r.now=fast;Date.now=fast;const timeout=r.setTimeout;r.setTimeout=(fn,ms)=>timeout(fn,ms/40);const interval=r.setInterval;r.setInterval=(fn,ms)=>interval(fn,Math.max(2,ms/40));}