'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
test('all four approved purple cutouts are committed real AVIF binaries with preserved provenance',()=>{const dir=path.join(__dirname,'../public/assets/games'),manifest=require('../public/assets/games/alpha-art-provenance.json');assert.equal(Object.keys(manifest.assets).length,4);let total=0;for(const [id,asset]of Object.entries(manifest.assets)){const bytes=fs.readFileSync(path.join(dir,asset.file));assert.equal(bytes.length,asset.bytes,id);assert.equal(bytes.toString('ascii',4,12),'ftypavif');const sha=crypto.createHash('sha1').update(Buffer.from('blob '+bytes.length+'\0')).update(bytes).digest('hex');assert.equal(sha,asset.blob,id);assert.match(asset.sourceSHA256,/^[a-f0-9]{64}$/);total+=bytes.length;}assert.ok(total<90*1024);});
